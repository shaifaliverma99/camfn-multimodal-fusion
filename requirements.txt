# Core
# NOTE: we tried pinning a CPU-only wheel via
# `--extra-index-url https://download.pytorch.org/whl/cpu` here, but
# Streamlit Community Cloud's build environment gets a 403 Forbidden
# querying that index (and the pinned version predated its Python's ABI
# tag anyway), so it's not usable in that specific host. Falling back to
# unpinned default-PyPI torch/torchvision: heavier download (CUDA libs
# bundled in even though unused on CPU-only hosts), but it actually
# installs. If you deploy somewhere that *can* reach download.pytorch.org,
# re-add the extra-index-url line above for a much smaller/faster install.
torch>=2.2
torchvision>=0.17
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
PyYAML>=6.0
Pillow>=10.0

# EEG I/O (reading .edf for CHB-MIT)
mne>=1.6

# Testing
pytest>=7.4

nibabel>=5.0   # available in this env; needed only by the Volumetric3DEncoder
               # extension path once real NIfTI (ADNI/OASIS-3) data exists

# Dashboard / API / reporting
fastapi>=0.110
uvicorn>=0.29
streamlit>=1.30
plotly>=5.20
reportlab>=4.0
python-multipart>=0.0.9  # required by FastAPI for file uploads

# Optional (not required by this skeleton, but referenced in docs as
# extension points):
# asrpy            # for full Artifact Subspace Reconstruction
# librosa          # for MFCC extraction from raw audio (voice modality here
#                    is already-tabular UCI features, so not required)
