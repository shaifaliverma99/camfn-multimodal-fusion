# Core
# CPU-only wheels: the default PyPI torch/torchvision wheels bundle NVIDIA
# CUDA libraries (multiple GB) that CPU-only hosts (Streamlit Community
# Cloud, most free-tier hosts) don't need and often can't even fit within
# their build size/time limits. This --extra-index-url pulls the small
# CPU-only build instead; on a machine that *does* have a GPU (like the
# one these checkpoints were trained on), install torch separately first
# per https://pytorch.org/get-started/locally/ if you want CUDA.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.1
torchvision==0.19.1
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
PyYAML>=6.0
Pillow>=10.0

# EEG I/O (reading .edf for CHB-MIT)
mne>=1.6

# Testing
pytest>=7.4

nibabel>=5.0   # available in this env; needed only by the Volumetric3DEncoder
               # extension path once real NIfTI (ADNI/OASIS-3) data exists

# Dashboard / API / reporting
fastapi>=0.110
uvicorn>=0.29
streamlit>=1.30
plotly>=5.20
reportlab>=4.0
python-multipart>=0.0.9  # required by FastAPI for file uploads

# Optional (not required by this skeleton, but referenced in docs as
# extension points):
# asrpy            # for full Artifact Subspace Reconstruction
# librosa          # for MFCC extraction from raw audio (voice modality here
#                    is already-tabular UCI features, so not required)
